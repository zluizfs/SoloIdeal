import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF', 
    flex: 1,
  },

  listItemStyle: {
    color: 'red',
  }
});

export default styles;